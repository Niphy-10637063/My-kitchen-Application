export enum HTTP_REQUEST_OPTION_OBSERVE {
    body = 'body',
    events = 'events',
    response = 'response'
  }
  
  export enum HTTP_REQUEST_OPTION_RESPONSE_TYPE {
    json = 'json',
    arrayBuffer = 'arraybuffer',
    text = 'text',
    blob = 'blob'
  }